<h1>Proyectos existentes</h1>

<table>
    <tr>
        <th>Titulo</th>
        <th>Area</th>
        <th>Descripción</th>
        <th>Problemática</th>
    </tr>
    
    <% @projects.each do |project|%>
    <tr>
        <td><%= project.titulo%></td>
        <td><%= project.area%></td>
        <td><%= project.descripcion%></td>
        <td><%= project.problematica%></td>
        <td><%= link_to 'Ver', project_path(project)%></td>
        
    </tr>
    <% end %>
</table>
