<h1> Editar pregunta <h1>
<%= form_for :pregunta, url: pregunta_path(@pregunta), method: :patch do |f| %>

 <% if @pregunta.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@pregunta.errors.count, "error") %> prohibited
        this pregunta from being saved:
      </h2>
      <ul>
        <% @pregunta.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  
  <p>
    <%= f.label :texto_pregunta %><br>
    <%= f.text_field :texto_pregunta%>
  </p>
 
  <p>
    <%= f.label :valor %><br>
    <%= f.text_area :valor %>
  </p>

  <p>
    <%= f.label :area %><br>
    <%= f.text_area :area %>
  </p>

  <p>
    <%= f.submit %>
  </p>
<% end %>


<%= form_for :pregunta, url: preguntas_path do |f| %>
  
<% end %>
 
<%= link_to 'Back', preguntas_path %>